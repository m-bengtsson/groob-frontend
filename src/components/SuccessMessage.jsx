import { StyledSuccessMessage } from "../styles/Container.styled";

const SuccessMessage = ({ message }) => {
  return <StyledSuccessMessage>{message}</StyledSuccessMessage>;
};

export default SuccessMessage;
